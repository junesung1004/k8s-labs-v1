apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort
  selector:
    app: frontend # frontend-pod의 라벨
  ports:
    - port: 80 # 외부에서 접근할 포트
      targetPort: 3000 # 컨테이너가 실제로 사용하는 포트
      nodePort: 30080 # 클러스터 외부에서 접근할 고정 포트 (30000~32767 범위)
